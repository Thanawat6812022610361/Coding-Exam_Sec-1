 #include <stdio.h>

 int total_hour (int hour[],int days){
 int total = 0;
 for (int i = 0; i < days; i++){
    total += hour[i];
 }
 return 0;
}
// คำนวณชั่วโมงรวมป้อนค่าเวลาทั้ง + กับจำนวนชั่วโมง
int L = 75;
int M = 120;
int H = 180;
printf ("Ot_Timewithpercent =", L*20/100);